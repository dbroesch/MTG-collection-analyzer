services:
  - type: web
    name: mtg-collection-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd website && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
